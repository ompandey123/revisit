<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Revisit</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
<div class="w-full h-full bg-zinc-900 p-10 text-white">
<h1 class="text-3xl tracking-tighter mt-5 ml-2 mb-5">All Users</h1>
<div>
    <a href="/" class="text-blue-500 ml-2">Go back to create</a>
</div>
    <div class="users flex gap-3 flex-wrap">
        <% if(users.length > 0) { %>
            <% users.forEach(function(user){ %>
        <div class="user w-72 p-4 bg-zinc-800 rounded-lg mt-5">
            <h3 class="text-xl tracking-tighter mt-3"><%= user.name %></h3>
            <h5 class="tracking-tighter mt-3"><%= user.email %></h5>
            <h5 class="tracking-tighter mt-3 mb-5"><%= user.contact %></h5>
            <div class="flex gap-3">
                <a href="/edit/<%= user._id %>" class="text-yellow-500">Edit</a>
                <a href="/delete/<%= user._id %>" class="text-red-500">Delete</a>
            </div>
        </div>
        <% }); %>
        <% } else { %>
            <h3 class="text-blue-500 ml-2 mt-5">No Users found!</h3>
        <% } %>
    </div>
</div>
</body>
</html>
